export interface PlaceData{
  placeAddress: string;
  placeAlias: string;
  placeName: string;
}

export interface Place {
  placeAddress: string;
  placeAlias: string;
  placeId: string;
  placeName: string;
}

export interface PlaceToDB {
  placeAddress: string;
  placeAlias: string;
  placeName: string;
  placeId: string
}